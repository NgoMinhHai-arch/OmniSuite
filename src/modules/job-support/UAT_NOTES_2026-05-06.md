# Job Support UAT Notes (2026-05-06)

## Scope validated

- Lint validation passed on all modified Job Support files.
- Integration artifacts created:
  - benchmark scorecard
  - foundation selection
  - domain contracts
  - adapter layer
  - workspace orchestrator
  - 3-workspace UI
  - safety guardrails (dry-run default, approval gate, daily live-apply cap)

## Runtime smoke observations

- Local Next server starts successfully.
- During UAT execution, all `/api/*` route calls returned `404` (including non-job-support API paths).
- This indicates an environment-level runtime routing issue outside the job-support feature logic itself.

## Expected behavior once API routing is restored

- `GET /api/job-support/providers` -> `{ ok: true, providers: [...] }`
- `POST /api/job-support/run` with:
  - `workspace=find-jobs` runs SerpApi aggregation (`vn-job-feed`)
  - `workspace=tailor-cv` runs `ai-resume-tailor` adapter flow
  - `workspace=auto-apply` in `live` mode without approval returns `MISSING_APPROVAL`

## Recommended follow-up

1. Fix baseline Next API routing issue.
2. Re-run the regression checklist in `REGRESSION_CHECKLIST.md`.
3. Capture final UAT outputs after API responses are reachable.
