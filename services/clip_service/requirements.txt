# Image pipeline CLIP (:8000) — light HTTP runtime only.
# Heavy AI dependencies are opt-in: install requirements-heavy.txt when CLIP search is needed.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pillow>=10.0.0
requests>=2.32.0
